# 🔍 Real-Time Object Detection with YOLOv8

This project uses the Ultralytics YOLOv8 model to detect objects in real-time from a webcam feed. It supports GPU acceleration, displays FPS and inference time, and allows optional video recording.

---

## 🚀 Features

- Real-time object detection using YOLOv8
- Supports GPU (CUDA) for faster inference
- FPS and inference time overlay
- Adjustable input size and confidence threshold
- Optional video recording

---

## 🛠️ Requirements

Install the required libraries with:

```bash
pip install -r requirements.txt
